#include "Dialog.h"
#include <iostream>

void main() {
    // Создаем объект-диспетчер
    Dialog D;

    // Запуск главного цикла обработки событий[cite: 1]
    D.Execute();
}

/*
   ПРОГРАММА, УПРАВЛЯЕМАЯ СОБЫТИЯМИ (Event-Driven Programming)

   АРХИТЕКТУРА:
   1. Иерархия классов: Object (абстрактный) -> Person -> Abiturient.
      Здесь используется механизм виртуальных функций для реализации
      позднего связывания.
   2. Группа (Tree): Реализована на базе динамической структуры "Дерево",
      хранящей указатели на базовый класс Object.
   3. Событийная логика: Объект TEvent служит контейнером для передачи
      команд между компонентами программы[cite: 1].
   4. Диспетчер (Dialog): Наследник Tree, который реализует главный цикл
      обработки (Execute) и распределяет команды[cite: 1].

   ШПАРГАЛКА ПО ТЕСТИРОВАНИЮ (Команды):
   'm' <число> — Создать/инициализировать группу[cite: 1].
   '+'         — Добавить элемент (Person или Abiturient)[cite: 1].
   '-'         — Удалить элемент из группы[cite: 1].
   's'         — Вывести информацию о всех объектах (Show)[cite: 1].
   'z'         — Вариант 13: Вычислить средний возраст всех Person в группе[cite: 1].
   'q'         — Конец работы (Quit)[cite: 1].
*/
